{
  "name": "@platform/sdk",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/collector.js",
  "types": "./dist/collector.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "git clean -xdf dist",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@platform/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}

{
  "name": "@platform/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "biz": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "types": "./dist/cli.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "git clean -xdf dist",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@platform/shared": "workspace:*",
    "@platform/core": "workspace:*",
    "@platform/compiler": "workspace:*",
    "@platform/cognition": "workspace:*",
    "@platform/runtime": "workspace:*",
    "@platform/recommendation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
